
# Notebooks

- notebooks 6, 7 and 8 are not used
- 9 fits model 
- 10 confidence for traces transitions
- 11 cooler, also has sort_metric (here you can use something else than 'pearsonr' e.g. 'spaermanr' )
- 12 fit model
  - uses build_model_from_config function to load a parameter dict
- 13 a b trnsitions
- 

In 12 change Normal to Gamma to make sure B vals cannot be positive and A vals cannot be nagative

eps er stepsize

storforksl på adfærd på normal and studentt. use normal, studentt does not work, Maybe try halfcauchy


in the 'with' statement you define a label vector to be used as dims below.