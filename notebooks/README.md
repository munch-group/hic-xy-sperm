
# Notebook

**nb01_compare_X_Y_eigs.ipynb**

- cooler, also has sort_metric (here you can use something else than 'pearsonr' e.g. 'spaermanr' )

**nb02_compare_X_Y_model.ipynb**

- fit model
- uses build_model_from_config function to load a parameter dict
- change Normal to Gamma to make sure B vals cannot be positive and A vals cannot be nagative

## nb03_compare_X_Y_eval.ipynb
- 13 a b trnsitions

**nb04_compare_X_Y_model_experiment.ipynb**



eps er stepsize

storforksl på adfærd på normal and studentt. use normal, studentt does not work, Maybe try halfcauchy


in the 'with' statement you define a label vector to be used as dims below.